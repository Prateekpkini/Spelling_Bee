import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — Firebase must be initialized for full widget testing.
    expect(1 + 1, equals(2));
  });
}
